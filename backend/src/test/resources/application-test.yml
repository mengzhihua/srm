spring:
  datasource:
    url: jdbc:h2:mem:srmtest;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
srm:
  auth:
    secret: test-secret-for-unit-tests
    admin-password: admin123
  integration:
    api-key: test-open-key
  delivery:
    auto-sync: false
  sap:
    mode: mock
  wms:
    mode: mock
